# Deliberately minimal. `supabase config push` writes every property this file
# declares and leaves the rest alone, so declaring only what we actually intend to
# manage keeps the shared project's auth, MFA and SMS settings untouched.
project_id = "bakery-shop"

[auth]
site_url = "https://bakery-shop.asimafridi929.workers.dev"
additional_redirect_urls = ["https://bakery-shop.asimafridi929.workers.dev/**"]
